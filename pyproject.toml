[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neuralclaw"
version = "0.4.1"
description = "Local Context OS for AI Agents"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "Max Sarlija", email = "max@sarlija.com"}
]
keywords = ["context", "memory", "ai", "agents", "local"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "typer>=0.12.0",
    "rich>=13.7.0",
    "pyyaml>=6.0",
    "cryptography>=42.0.0",
    "click>=8.1.0",
    "appdirs>=1.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.3.0",
    "click>=8.1.0",
]
api = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "jinja2>=3.1.0",
]
tui = [
    "rich>=13.7.0",
]
ollama = [
    "requests>=2.31.0",
]
embeddings = [
    "requests>=2.31.0",
    "numpy>=1.26.0",
]
full = [
    "requests>=2.31.0",
    "numpy>=1.26.0",
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "jinja2>=3.1.0",
]


[project.scripts]
neuralclaw = "neuralclaw.cli.main:app"

[project.entry-points."neuralclaw.plugins"]
audit = "neuralclaw.plugins.builtins.audit:hooks"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]